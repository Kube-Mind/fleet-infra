enabled: true
installCRDs: true

resources:
  requests:
    cpu: 50m
    memory: 64Mi
  limits:
    cpu: 200m
    memory: 256Mi

featureGates: "ExperimentalGatewayAPISupport=true"

controller:
  extraArgs:
    - --feature-gates=ExperimentalGatewayAPISupport=true