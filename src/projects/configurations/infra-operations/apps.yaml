spec:
  project: infra-operations

apps:
  k3s-upgrader-plans:
    # renovate bot sequenced fields
    installType: localKustomize
    # chart required fields
    automated: true
    syncWave: "20"

  letsencrypt:
    # renovate bot sequenced fields
    installType: localKustomize
    # chart required fields
    automated: true
    syncWave: "20"

  metallb-runtime-configs:
    # renovate bot sequenced fields
    installType: localKustomize
    # chart required fields
    automated: false
    syncWave: "10"

  secrets-integrator:
    # renovate bot sequenced fields
    installType: localHelm
    # chart required fields
    automated: false
    syncWave: "20"
