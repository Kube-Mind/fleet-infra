{{- $appNamespace := "argo-cd" -}}
{{- $destinationServer := .Values.spec.destination.server -}}
{{- $appProject := .Values.spec.project -}}
{{- $repoURL := .Values.spec.source.repoURL -}}
{{- $targetRevision := .Values.spec.source.targetRevision -}}

{{- range $value := .Values.apps }}
---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: {{ $value.name }}
  namespace: {{ $appNamespace }}
  annotations:
    argocd.argoproj.io/sync-wave: {{ $value.syncWave | default "99" | quote }}
  labels:
    app: {{ $value.name }}
    argocd.argoproj.io/appproject: {{ $appProject }}
    {{ $appProject }}/installType: {{ $value.installType }}
    {{- if $value.extraLabels }}
    {{- toYaml $value.extraLabels | nindent 4 }}
    {{- end }}
  finalizers:
    - resources-finalizer.argocd.argoproj.io
    {{- if $value.additionalFinalizers }}
    {{ toYaml $value.additionalFinalizers | nindent 4 }}
    {{- end }}
spec:
  destination:
    namespace: {{ $value.namespace | default $value.name }}
    server: {{ $destinationServer }}
  project: {{ $appProject }}
  sources:
  {{- if or (eq $value.installType "localKustomize") (eq $value.installType "localHelm") }}
    - repoURL: {{ $repoURL }}
      path: "src/projects/configurations/{{ $appProject }}/{{ $value.name }}"
      targetRevision: {{ $targetRevision }}
  {{- else }}
    - repoURL: {{ $value.repoURL }}
      targetRevision: {{ $value.targetRevision }}
      helm:
        valueFiles:
          - $values/src/projects/configurations/{{ $appProject }}/{{ $value.name }}/values.yaml
  {{- if eq $value.installType "remoteHelm" }}
      path: {{ $value.path }}
  {{- else }}
      chart: {{ $value.chartName }}
  {{- end }}
    - repoURL: {{ $repoURL }}
      targetRevision: {{ $targetRevision }}
      ref: values
  {{- end }}
  syncPolicy:
    {{- if $value.automated }}
    automated:
      selfHeal: true
    {{- end }}
    syncOptions:
      - CreateNamespace=true
      - ApplyOutOfSyncOnly=true
      - ServerSideApply=true
      - PruneLast=true
      - RespectIgnoreDifferences=true
    {{- range $option := $value.extraSyncOptions | default dict }}
      - {{ toYaml $option }}
    {{- end }}
  {{- if $value.ignoreDifferences }}
  ignoreDifferences: {{ toYaml $value.ignoreDifferences | nindent 4 }}
  {{- end }}
{{- end }}
