{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:base",
    "config:recommended",
    "mergeConfidence:all-badges"
  ],
  "dependencyDashboard": true,
  "labels": [
    "dependencies"
  ],
  "kustomize": {
    "managerFilePatterns": [
      "src/**/*.yaml"
    ]
  },
  "helm-values": {
    "managerFilePatterns": [
      "src/**/*.yaml",
      "src/**/*.values.yaml"
    ]
  },
  "enabledManagers": [
    "kustomize",
    "helm-values",
    "kubernetes",
    "custom.regex"
  ],
  "kubernetes": {
    "managerFilePatterns": [
      "src/**/*.yaml",
      "src/**/*.yml"
    ]
  },
  "packageRules": [
    {
      "matchManagers": [
        "kustomize",
        "helm-values",
        "kubernetes",
        "custom.regex"
      ],
      "matchFileNames": [
        "src/platform/observability/**"
      ],
      "groupName": "chore(deps): update platform/observability helm charts and oci"
    },
  "packageRules": [
    {
      "matchManagers": [
        "kustomize",
        "helm-values",
        "kubernetes",
        "custom.regex"
      ],
      "matchFileNames": [
        "src/workloads/**"
      ],
      "groupName": "chore(deps): update workloads helm charts and oci"
    },
    {
      "matchManagers": [
        "kustomize",
        "helm-values",
        "kubernetes",
        "custom.regex"
      ],
      "matchFileNames": [
        "src/home/**"
      ],
      "groupName": "chore(deps): update home helm charts and oci"
    }
  ],
  "customManagers": [
    {
      "customType": "regex",
      "managerFilePatterns": [
        "/kustomization.yaml/"
      ],
      "matchStrings": [
        "https://raw.githubusercontent.com/(?<depName>[^/]*/[^/]*)/(?<currentValue>.*?)/"
      ],
      "datasourceTemplate": "github-tags"
    },
    {
      "customType": "regex",
      "managerFilePatterns": [
        "**/plans/agent.yaml",
        "**/plans/server.yaml"
      ],
      "matchStrings": [
        "version:\\s*(?<currentValue>v[0-9]+\\.[0-9]+\\.[0-9]+\\+k3s[0-9]+)"
      ],
      "depNameTemplate": "k3s-io/k3s-upgrade",
      "datasourceTemplate": "github-tags"
    }
  ]
}
