{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": ["config:recommended"],
  "dependencyDashboard": true,
  "labels": ["dependencies"],
  "enabledManagers": ["custom.regex"],
  "packageRules": [
    {
      "matchManagers": ["custom.regex"],
      "groupName": "Helm and OCI Chart Versions"
    }
  ],
  "customManagers": [
    {
      "customType": "regex",
      "managerFilePatterns": ["**/apps.yaml"],
      "matchStrings": [
        "(?sm)^chartName:\\s(?<chartName>\\S+?)\\s*\\n.*?installType:\\shelm.*?repoURL:\\s(?<repoURL>\\S+).*?targetRevision:\\s(?<currentValue>\\S+?)(?=\\nchartName:|\\z)"
      ],
      "depNameTemplate": "{{{ chartName }}}",
      "registryUrlTemplate": "{{{ repoURL }}}",
      "datasourceTemplate": "helm"
    },
    {
      "customType": "regex",
      "managerFilePatterns": ["**/apps.yaml"],
      "matchStrings": [
        "(?sm)^chartName:\\s(?<chartName>\\S+?)\\s*\\n.*?installType:\\soci.*?repoURL:\\s(?<repoURL>\\S+).*?targetRevision:\\s(?<currentValue>\\S+?)(?=\\nchartName:|\\z)"
      ],
      "depNameTemplate": "{{{ repoURL }}}/{{{ chartName }}}",
      "datasourceTemplate": "docker"
    }
  ]
}
