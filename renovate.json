{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": ["config:recommended"],
  "dependencyDashboard": true,
  "labels": ["dependencies"],
  "enabledManagers": ["custom.regex"],
  "packageRules": [
    {
      "matchManagers": ["custom.regex"],
      "groupName": "Helm and OCI Chart Versions"
    }
  ],
  "customManagers": [
    {
      "customType": "regex",
      "managerFilePatterns": ["**/apps.yaml"],
      "matchStrings": [
        "chartName: (?<chartName>\\S+) installType: helm repoURL: (?<repoURL>\\S+) targetRevision: (?<currentValue>\\S+)"
      ],
      "depNameTemplate": "{{{ chartName }}}",
      "registryUrlTemplate": "{{{ repoURL }}}",
      "datasourceTemplate": "helm"
    },
    {
      "customType": "regex",
      "managerFilePatterns": ["**/apps.yaml"],
      "matchStrings": [
        "chartName: (?<chartName>\\S+) installType: oci repoURL: (?<repoURL>\\S+) targetRevision: (?<currentValue>\\S+)"
      ],
      "depNameTemplate": "{{{ repoURL }}}/{{{ chartName }}}",
      "datasourceTemplate": "docker"
    }
  ]
}
